//check the duplicate number at the kth distance
//TC-O(n) SC-O(1)
/*#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    vector<int>v={1,2,3,4,2,5};
    int k;
    cin>>k;
    for(int i=0;i<v.size();i++)
    {
        if(i+k<v.size())
        {
            if(v[i]==v[i+k])
            cout<<v[i]<<"\n";
        }
    }

return 0;
}*/




//optimal solution 
#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);




    return 0;
}